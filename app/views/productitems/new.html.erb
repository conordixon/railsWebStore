<%= form_with scope: :productitem, url: productitems_path, local: true do |form| %>

  <% if @productitem.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@productitem.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @productitem.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :productname %>
    <%= form.text_field :productname %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>

  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
<%= link_to 'Back', productitems_path %>