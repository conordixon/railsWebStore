<div class="mainCont">
<%= form_with(model: productitem, local: true) do |form| %>
  <% if productitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(productitem.errors.count, "error") %> prohibited this productitem from being saved:</h2>

      <ul>
        <% productitem.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :productname %>
    <%= form.text_field :productname, id: :productitem_name, :type =>"text"%>
  </div>
  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :productitem_description, :type =>"text" %>
  </div>
  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :productitem_price, :type =>"text" %>
  </div>
  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand, id: :productitem_brand, :type =>"text" %>
  </div>
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
</div>
